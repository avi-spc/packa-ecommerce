import OrderMeta from "./OrderMeta";

const OrdersHistory = () => {
    return (
        <section className="section-orders-history">
            <OrderMeta />
        </section>
    );
};

export default OrdersHistory;
